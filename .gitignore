# Build products
.build/
.build/artifacts/
DerivedData/
.swiftpm/xcode/

# App bundles + release artifacts (generated by the packaging scripts)
*.app/
*.zip
*.dSYM*
*.dmg
*.pkg
Icon.icns
Icon.iconset

# macOS noise
.DS_Store

# Local secrets (never commit signing/notary material)
*.p8
Scripts/local.env

# Note: version.env, appcast.xml, CHANGELOG.md, Scripts/config.env ARE committed.
